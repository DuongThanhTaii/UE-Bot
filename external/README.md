# External Dependencies

External repositories integrated into this project.

## Moltbot

Moltbot is added as a git submodule:

```bash
# Initialize
git submodule update --init --recursive

# Update to latest
git submodule update --remote external/moltbot
```
